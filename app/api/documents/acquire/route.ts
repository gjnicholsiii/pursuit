import { put } from "@vercel/blob";
import * as cheerio from "cheerio";
import { NextRequest, NextResponse } from "next/server";
import { getSql } from "@/lib/db";
import { requireInternalAuth } from "@/lib/internal-auth";

export const dynamic = "force-dynamic";
export const maxDuration = 300;

const STANDARD_DOCUMENT_BYTES = 24 * 1024 * 1024;
const MAX_DOCUMENT_BYTES = 32 * 1024 * 1024;
const ACQUISITION_BATCH_SIZE = 4;

type PendingDocumentRow = { job_id:string; id:string; opportunity_id:string; source_url:string; opportunity_source_url:string; filename:string; source_family:string; host_class:string; priority:number; last_error:string|null; meta:Record<string,unknown>|null };
function filenameFromDisposition(value:string|null){if(!value)return null;const utf8=value.match(/filename\*=UTF-8''([^;]+)/i);if(utf8?.[1]){try{return decodeURIComponent(utf8[1])}catch{return utf8[1]}}return value.match(/filename="?([^";]+)"?/i)?.[1]||null}
function safeFilename(value:string){return value.replace(/\+/g," ").replace(/[^a-zA-Z0-9._() -]+/g,"-").replace(/\s+/g," ").trim()||"document"}
function normalizedName(value:string){return safeFilename(value).toLowerCase().replace(/\s+/g," ").trim()}
function looksValid(bytes:ArrayBuffer,contentType:string,filename:string){const head=String.fromCharCode(...new Uint8Array(bytes.slice(0,8)));const lowerType=contentType.toLowerCase();const lowerName=filename.toLowerCase();if(lowerType.includes("text/html")||/^\s*</.test(head))return false;if(lowerName.endsWith(".pdf")||lowerType.includes("application/pdf"))return head.startsWith("%PDF");if(/\.(docx|xlsx|pptx|zip)$/i.test(lowerName))return head.startsWith("PK");return bytes.byteLength>0}
async function finishJob(jobId:string){await getSql().query(`update document_jobs set state='done',leased_until=null,lease_owner=null,updated_at=now() where id=$1::bigint`,[jobId])}
async function failJob(jobId:string,error:string,permanent=false){await getSql().query(permanent?`update document_jobs set state='dead',attempts=max_attempts,leased_until=null,lease_owner=null,last_error=$2,updated_at=now() where id=$1::bigint`:`update document_jobs set state=case when attempts>=max_attempts then 'dead' else 'pending' end,run_after=now()+(interval '1 second'*least(600,power(2,least(attempts,10)))),leased_until=null,lease_owner=null,last_error=$2,updated_at=now() where id=$1::bigint`,[jobId,error.slice(0,1000)])}
function cookieHeader(headers:Headers){const extended=headers as Headers&{getSetCookie?:()=>string[]};const values=extended.getSetCookie?.()||(headers.get("set-cookie")?[headers.get("set-cookie") as string]:[]);const cookies:string[]=[];for(const value of values){const matches=value.match(/(?:^|,\s*)([A-Za-z0-9_.-]+=[^;,\s]+)/g)||[];for(const match of matches){const cleaned=match.replace(/^,\s*/,"");if(cleaned&&!cookies.includes(cleaned))cookies.push(cleaned)}}return cookies.join("; ")}
async function fetchIonWave(document:PendingDocumentRow,signal:AbortSignal,baseHeaders:Record<string,string>){const bootstrap=await fetch(document.opportunity_source_url,{redirect:"follow",signal,headers:{...baseHeaders,Accept:"text/html,application/xhtml+xml,*/*"},cache:"no-store"});if(!bootstrap.ok)return bootstrap;const cookie=cookieHeader(bootstrap.headers);const $=cheerio.load(await bootstrap.text());const wanted=normalizedName(document.filename);let freshUrl=document.source_url;for(const row of $("#ctl00_mainContent_rgBidAttachments_ctl00 tr.rgRow, #ctl00_mainContent_rgBidAttachments_ctl00 tr.rgAltRow").toArray()){const firstCell=$(row).find("td").first();const rawName=firstCell.text().replace(/\(please login to view this document\)/i,"").trim();if(normalizedName(rawName)!==wanted)continue;const href=firstCell.find("a[href]").first().attr("href");if(href)try{freshUrl=new URL(href,bootstrap.url||document.opportunity_source_url).toString()}catch{}break}if(freshUrl!==document.source_url)await getSql().query(`update opportunity_documents set source_url=$2 where id=$1::uuid`,[document.id,freshUrl]);const headers:Record<string,string>={...baseHeaders,Referer:bootstrap.url||document.opportunity_source_url,Accept:"application/pdf,application/octet-stream,*/*"};if(cookie)headers.Cookie=cookie;return fetch(freshUrl,{redirect:"follow",signal,headers,cache:"no-store"})}
async function fetchJaggaer(document:PendingDocumentRow,signal:AbortSignal,baseHeaders:Record<string,string>){const page=await fetch(document.opportunity_source_url,{redirect:"follow",signal,headers:{...baseHeaders,Accept:"text/html,application/xhtml+xml,*/*"},cache:"no-store"});if(!page.ok)return page;const cookie=cookieHeader(page.headers);const html=await page.text();const $=cheerio.load(html);const wanted=normalizedName(document.filename);let freshUrl="";$("a[href]").each((_,node)=>{if(freshUrl)return;const href=$(node).attr("href")||"";if(!/solutions-selectsite-documents\.s3\.amazonaws\.com/i.test(href))return;const name=normalizedName($(node).text()||href.split("/").pop()?.split("?")[0]||"");if(name===wanted||href.includes(encodeURIComponent(document.filename))||href.includes(document.filename)){try{freshUrl=new URL(href,page.url).toString()}catch{}}});if(!freshUrl){const key=document.source_url.match(/\/Sourcingevent\/([^?]+)/i)?.[1];if(key){const match=html.match(new RegExp(`https?:[^"'<>\\s]+/Sourcingevent/${key.replace(/[.*+?^${}()|[\]\\]/g,"\\$&")}[^"'<>\\s]*`,`i`));if(match)freshUrl=match[0].replace(/&amp;/g,"&")}}if(!freshUrl)return new Response("JAGGAER signed document URL could not be refreshed",{status:404});await getSql().query(`update opportunity_documents set source_url=$2,extraction_status='cataloged' where id=$1::uuid`,[document.id,freshUrl]);const headers:Record<string,string>={...baseHeaders,Referer:page.url,Accept:"application/pdf,application/octet-stream,*/*"};if(cookie)headers.Cookie=cookie;return fetch(freshUrl,{redirect:"follow",signal,headers,cache:"no-store"})}
function periscopeDetailUrl(document:PendingDocumentRow){for(const candidate of [document.opportunity_source_url,document.source_url]){try{const url=new URL(candidate);if(!/\/bso\/external\/bidDetail\.sda$/i.test(url.pathname))continue;url.searchParams.delete("downloadFileNbr");url.searchParams.delete("mode");url.searchParams.delete("fn");return url.toString()}catch{}}return document.opportunity_source_url}
async function fetchPeriscope(document:PendingDocumentRow,signal:AbortSignal,baseHeaders:Record<string,string>){const detailUrl=periscopeDetailUrl(document);const page=await fetch(detailUrl,{redirect:"follow",signal,headers:{...baseHeaders,Accept:"text/html,application/xhtml+xml,*/*"},cache:"no-store"});if(!page.ok)return page;const cookie=cookieHeader(page.headers);const html=await page.text();const $=cheerio.load(html);const wanted=normalizedName(document.filename);let fileNbr="";$("a,button,[onclick]").each((_,node)=>{if(fileNbr)return;const el=$(node);const text=normalizedName(el.text());const raw=[el.attr("onclick")||"",el.attr("href")||""].join(" ");const match=raw.match(/downloadFile\s*\(\s*['\"]?(\d+)['\"]?\s*\)/i)||raw.match(/[?&]downloadFileNbr=(\d+)/i);if(!match)return;if(text===wanted||text.includes(wanted)||wanted.includes(text)||normalizedName(raw).includes(wanted))fileNbr=match[1]});if(!fileNbr){for(const match of html.matchAll(/downloadFile\s*\(\s*['\"]?(\d+)['\"]?\s*\)/gi)){const start=Math.max(0,(match.index||0)-500);const end=Math.min(html.length,(match.index||0)+500);if(normalizedName(html.slice(start,end)).includes(wanted)){fileNbr=match[1];break}}}if(!fileNbr)return new Response("Periscope attachment number could not be resolved",{status:404});const url=new URL(page.url);url.searchParams.set("downloadFileNbr",fileNbr);url.searchParams.set("mode","download");url.searchParams.set("fn",document.filename);const freshUrl=url.toString();await getSql().query(`update opportunity_documents set source_url=$2,extraction_status='cataloged' where id=$1::uuid`,[document.id,freshUrl]);const headers:Record<string,string>={...baseHeaders,Referer:page.url,Accept:"application/pdf,application/octet-stream,*/*"};if(cookie)headers.Cookie=cookie;return fetch(freshUrl,{redirect:"follow",signal,headers,cache:"no-store"})}
async function fetchDocument(document:PendingDocumentRow,signal:AbortSignal){const baseHeaders={"User-Agent":"Mozilla/5.0 (compatible; Pursuit/1.0; +https://pursuit-neon.vercel.app)",Accept:"*/*"};const isSamResource=/sam\.gov\/api\/prod\/opps\/v3\/opportunities\/resources\/files\//i.test(document.source_url);if(isSamResource){const apiKey=process.env.SAM_GOV_API_KEY;if(apiKey){const url=new URL(document.source_url);if(!url.searchParams.has("api_key"))url.searchParams.set("api_key",apiKey);return fetch(url,{redirect:"follow",signal,headers:baseHeaders,cache:"no-store"})}}if(/app0?1\.jaggaer\.com|solutions-selectsite-documents\.s3\.amazonaws\.com/i.test(document.opportunity_source_url+" "+document.source_url)){try{return await fetchJaggaer(document,signal,baseHeaders)}catch{}}if(/\/bso\/external\/bidDetail\.sda/i.test(document.opportunity_source_url+" "+document.source_url)){try{return await fetchPeriscope(document,signal,baseHeaders)}catch{}}if(document.host_class==="ionwave"){try{return await fetchIonWave(document,signal,baseHeaders)}catch{return fetch(document.source_url,{redirect:"follow",signal,headers:{...baseHeaders,Referer:document.opportunity_source_url},cache:"no-store"})}}return fetch(document.source_url,{redirect:"follow",signal,headers:baseHeaders,cache:"no-store"})}
async function readBounded(response:Response,limit:number){if(!response.body)return response.arrayBuffer();const reader=response.body.getReader();const chunks:Uint8Array[]=[];let total=0;while(true){const {done,value}=await reader.read();if(done)break;if(!value)continue;total+=value.byteLength;if(total>limit){await reader.cancel();throw new Error("too_large_runtime_cap")}chunks.push(value)}const merged=new Uint8Array(total);let offset=0;for(const chunk of chunks){merged.set(chunk,offset);offset+=chunk.byteLength}return merged.buffer}
async function acquireOne(document:PendingDocumentRow){const sql=getSql();const controller=new AbortController();const timeout=setTimeout(()=>controller.abort(),20000);try{const response=await fetchDocument(document,controller.signal);if(!response.ok){const permanent=[400,401,404,410].includes(response.status);await sql.query(`update opportunity_documents set extraction_status=$2 where id=$1`,[document.id,permanent?"fetch_failed":"cataloged"]);await failJob(document.job_id,`http_${response.status}`,permanent);return{ok:false,documentId:document.id,status:response.status}}const contentLength=Number(response.headers.get("content-length")||0);if(contentLength>MAX_DOCUMENT_BYTES){await sql.query(`update opportunity_documents set extraction_status='fetch_too_large' where id=$1`,[document.id]);await failJob(document.job_id,"too_large_runtime_cap",true);return{ok:false,documentId:document.id,status:413}}let bytes:ArrayBuffer;try{bytes=await readBounded(response,MAX_DOCUMENT_BYTES)}catch(error){if(error instanceof Error&&error.message==="too_large_runtime_cap"){await sql.query(`update opportunity_documents set extraction_status='fetch_too_large' where id=$1`,[document.id]);await failJob(document.job_id,"too_large_runtime_cap",true);return{ok:false,documentId:document.id,status:413}}throw error}const contentType=response.headers.get("content-type")||"application/octet-stream";const filename=safeFilename(filenameFromDisposition(response.headers.get("content-disposition"))||document.filename);if(!looksValid(bytes,contentType,filename)){await sql.query(`update opportunity_documents set extraction_status='fetch_invalid' where id=$1`,[document.id]);await failJob(document.job_id,"invalid_payload",true);return{ok:false,documentId:document.id,status:422}}const family=document.source_family==="sled"?"sled":"sam";const pathname=`${family}/${document.opportunity_id}/${document.id}/${filename}`;const blob=await put(pathname,bytes,{access:"private",contentType,addRandomSuffix:false,allowOverwrite:true,multipart:bytes.byteLength>STANDARD_DOCUMENT_BYTES});await sql.query(`update opportunity_documents set filename=$2,storage_key=$3,fetched_at=now(),extraction_status='fetched' where id=$1`,[document.id,filename,blob.pathname]);if(/\.pdf$/i.test(filename))await sql.query(`insert into document_jobs(document_id,stage,host_class,priority,meta) values($1::uuid,'extract',$2,$3,$4::jsonb) on conflict(document_id,stage) do update set priority=least(document_jobs.priority,excluded.priority),meta=coalesce(document_jobs.meta,'{}'::jsonb)||excluded.meta,state=case when document_jobs.state in ('done','leased') then document_jobs.state else 'pending' end,updated_at=now()`,[document.id,document.host_class,document.priority,JSON.stringify({...document.meta,bytes:bytes.byteLength})]);await finishJob(document.job_id);return{ok:true,documentId:document.id,bytes:bytes.byteLength,analysisQueued:/\.pdf$/i.test(filename)}}catch(error){const message=error instanceof Error?error.message:"fetch_failed";await failJob(document.job_id,message,false);return{ok:false,documentId:document.id,reason:message}}finally{clearTimeout(timeout)}}
export async function GET(request:NextRequest){const unauthorized=requireInternalAuth(request);if(unauthorized)return unauthorized;if(!process.env.BLOB_STORE_ID)return NextResponse.json({ok:false,error:"BLOB_STORE_ID is not available to this deployment"},{status:503});const sql=getSql();await sql.query(`update document_jobs j set state='dead',leased_until=null,lease_owner=null,last_error='opportunity_closed_or_expired',updated_at=now() from opportunity_documents d,opportunities o where j.document_id=d.id and d.opportunity_id=o.id and j.stage='acquire' and j.state='pending' and d.storage_key is null and d.is_missing=false and not (lower(coalesce(o.status,'')) in ('open','active','posted') and (o.due_at is null or o.due_at>=now()))`);await sql.query(`update document_jobs set state=case when attempts>=max_attempts then 'dead' else 'pending' end,run_after=now()+(interval '1 second'*least(600,power(2,least(attempts,10)))),leased_until=null,lease_owner=null,last_error=coalesce(last_error,'lease expired'),updated_at=now() where state='leased' and leased_until<now()`);const owner=`vercel-acquire-${crypto.randomUUID()}`;const rows=await sql.query(`with claim as (select j.id from document_jobs j join opportunity_documents d on d.id=j.document_id join opportunities o on o.id=d.opportunity_id where j.stage='acquire' and j.state='pending' and j.run_after<=now() and d.storage_key is null and d.is_missing=false and lower(coalesce(o.status,'')) in ('open','active','posted') and (o.due_at is null or o.due_at>=now()) order by j.priority,j.run_after,j.id limit ${ACQUISITION_BATCH_SIZE} for update skip locked), leased as (update document_jobs j set state='leased',leased_until=now()+interval '10 minutes',lease_owner=$1,attempts=attempts+1,updated_at=now() from claim where j.id=claim.id returning j.id as job_id,j.document_id,j.host_class,j.priority,j.last_error,j.meta) select leased.job_id::text,d.id,d.opportunity_id,d.source_url,o.source_url as opportunity_source_url,d.filename,s.source_family,leased.host_class,leased.priority,leased.last_error,leased.meta from leased join opportunity_documents d on d.id=leased.document_id join opportunities o on o.id=d.opportunity_id join sources s on s.id=o.source_id`,[owner]) as PendingDocumentRow[];if(!rows.length)return NextResponse.json({ok:true,processed:0,message:"No acquisition jobs are waiting"});const results:Array<Record<string,unknown>>=[];const fast=rows.filter(r=>r.host_class!=="ionwave");const slow=rows.filter(r=>r.host_class==="ionwave");for(let i=0;i<fast.length;i+=4)results.push(...await Promise.all(fast.slice(i,i+4).map(acquireOne)));for(const row of slow)results.push(await acquireOne(row));return NextResponse.json({ok:true,processed:results.length,results})}