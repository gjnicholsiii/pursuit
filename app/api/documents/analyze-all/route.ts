import { get } from "@vercel/blob";
import { NextRequest, NextResponse } from "next/server";
import { getSql } from "@/lib/db";
import { requireInternalAuth } from "@/lib/internal-auth";

export const dynamic = "force-dynamic";
export const maxDuration = 60;

const ANALYZER_VERSION=4;
type Row={job_id:string;id:string;opportunity_id:string;filename:string;text_storage_key:string};
type Req={category:string;text:string;line:number};

function compact(v:string){return v.replace(/\u0000/g,"").replace(/\s+/g," ").trim()}
function skipRequirementMining(filename:string){const n=filename.toLowerCase().replace(/[^a-z0-9]+/g," ").trim();return /(^| )(w 9|w9|form 1295|1295 form|certificate of interested parties)( |$)/.test(n)}
function segments(text:string){const out:Array<{text:string;line:number}>=[];text.split(/\r?\n/).forEach((page,pageIndex)=>{const normalized=compact(page);if(!normalized)return;const parts=normalized.split(/(?<=[.;!?])\s+(?=(?:\(?\d+[.)]|[A-Z•]))|\s+(?=(?:SECTION|ARTICLE|PART)\s+\d+)/g);let buffer="";for(const part of parts){const p=compact(part);if(!p)continue;if((buffer+" "+p).trim().length<=420){buffer=(buffer+" "+p).trim();continue}if(buffer.length>=20)out.push({text:buffer,line:pageIndex+1});buffer=p}if(buffer.length>=20)out.push({text:buffer,line:pageIndex+1})});return out}
function preAward(line:string){return /\b(bidder|offeror|proposer|respondent|vendor)\b|\b(to submit (?:a )?(?:bid|proposal|offer)|with (?:the )?(?:bid|proposal|offer)|at (?:the )?time of (?:bid|proposal|offer)|condition of (?:bid|award)|eligible|eligibility|prequalif|responsive|responsible bidder|prior to award|before award)\b/i.test(line)}
function mandatory(line:string){return /\b(shall|must|required|mandatory|minimum|condition of award|condition of bid|eligible|eligibility|prequalif|responsib(?:le|ility)|as a prerequisite)\b/i.test(line)&&!/\b(if required|when required by|not required|is not required|are not required)\b/i.test(line)}
function classify(line:string):string|null{
  if(/\b(bid bond|proposal bond|bid security|bonding capacity|surety)\b/i.test(line)&&preAward(line))return"bonding";
  if(/\b(performance bond|payment bond)\b/i.test(line)&&/\b(condition of award|prior to award|successful bidder|awarded contractor)\b/i.test(line))return"bonding";
  if(/\b(insurance|certificate of insurance|general liability|workers.? compensation|automobile liability|umbrella)\b/i.test(line)&&preAward(line))return"insurance";
  if(/\b(license[ds]?|licensure|certif(?:y|ied|ication)|registered contractor|professional registration)\b/i.test(line)&&preAward(line))return"certification";
  if(/\b(mandatory|required)\b.{0,100}\b(pre[- ]bid (?:meeting|conference)|preproposal (?:meeting|conference)|pre-proposal (?:meeting|conference)|site visit|walkthrough)\b|\b(pre[- ]bid (?:meeting|conference)|preproposal (?:meeting|conference)|pre-proposal (?:meeting|conference)|site visit|walkthrough)\b.{0,100}\b(mandatory|required|must attend|shall attend)\b/i.test(line))return"site_visit";
  if(/\b(years? of experience|past performance|similar projects?|minimum experience|references?|key personnel|resume)\b/i.test(line)&&preAward(line))return"performance";
  if(/\b(authorized dealer|manufacturer authorization|approved manufacturer|minimum technical qualification|technical qualification|required capability)\b/i.test(line)&&preAward(line))return"technical";
  return null;
}
function collectRequirements(text:string):Req[]{const out:Req[]=[];const seen=new Set<string>();for(const segment of segments(text)){const line=segment.text;if(line.length<25||line.length>500||!mandatory(line))continue;if(/\b(internal revenue service|taxpayer identification|social security number|tax return|construction activities|commencement of work|during construction|after award|after notice to proceed|prior to commencing|shop drawing|submittal|storm water|dewatering|traffic control|environmental protection|wetlands?|archaeolog|historic objects?|wildlife|erosion|dust control|utility lines?|work zone|permittee of record)\b/i.test(line)&&!/\b(condition of award|condition of bid|bidder|offeror|proposer|respondent|vendor|bid bond|proposal bond|bid security)\b/i.test(line))continue;const category=classify(line);if(!category)continue;const key=line.toLowerCase();if(seen.has(key))continue;seen.add(key);out.push({category,text:line,line:segment.line});if(out.length>=30)break}return out}

async function finishJob(jobId:string){await getSql().query(`update document_jobs set state='done',leased_until=null,lease_owner=null,last_error=null,updated_at=now() where id=$1::bigint`,[jobId])}
async function retryJob(jobId:string,error:string){await getSql().query(`update document_jobs set state=case when attempts>=max_attempts then 'dead' else 'pending' end,run_after=now()+(interval '1 second'*least(600,power(2,least(attempts,10)))),leased_until=null,lease_owner=null,last_error=$2,updated_at=now() where id=$1::bigint`,[jobId,error.slice(0,1000)])}

async function analyzeOne(document:Row){const sql=getSql();try{if(skipRequirementMining(document.filename)){await sql.query(`delete from requirements where document_id=$1::uuid and normalized_value->>'source'='document_text'`,[document.id]);await sql.query(`update opportunity_documents set extraction_status='analyzed' where id=$1::uuid`,[document.id]);await finishJob(document.job_id);return{ok:true,documentId:document.id,filename:document.filename,requirementsFound:0,skippedBoilerplate:true}}const blob=await get(document.text_storage_key,{access:"private"});if(!blob||blob.statusCode!==200||!blob.stream)throw new Error("extracted_text_unavailable");const text=(await new Response(blob.stream).text()).replace(/\u0000/g,"");const requirements=collectRequirements(text);await sql.query(`delete from requirements where document_id=$1::uuid and normalized_value->>'source'='document_text'`,[document.id]);if(requirements.length)await sql.query(`insert into requirements(opportunity_id,document_id,category,requirement_text,mandatory,evidence_locator,normalized_value,extraction_confidence) select $1::uuid,$2::uuid,u.category,u.requirement_text,true,jsonb_build_object('document_id',$2::text,'page',u.line),jsonb_build_object('source','document_text','analyzer_version',$6::int),0.98 from unnest($3::text[],$4::text[],$5::int[]) as u(category,requirement_text,line)`,[document.opportunity_id,document.id,requirements.map(r=>r.category),requirements.map(r=>r.text),requirements.map(r=>r.line),ANALYZER_VERSION]);await sql.query(`update opportunity_documents set extraction_status='analyzed' where id=$1::uuid`,[document.id]);await finishJob(document.job_id);return{ok:true,documentId:document.id,filename:document.filename,requirementsFound:requirements.length}}catch(error){const message=error instanceof Error?error.message:"evidence_analysis_failed";await retryJob(document.job_id,message);return{ok:false,documentId:document.id,reason:message}}}

export async function GET(request:NextRequest){const unauthorized=requireInternalAuth(request);if(unauthorized)return unauthorized;const sql=getSql();await sql.query(`update document_jobs set state=case when attempts>=max_attempts then 'dead' else 'pending' end,run_after=now()+(interval '1 second'*least(600,power(2,least(attempts,10)))),leased_until=null,lease_owner=null,last_error=coalesce(last_error,'lease expired'),updated_at=now() where state='leased' and leased_until<now()`);const owner=`vercel-analyze-${crypto.randomUUID()}`;const rows=await sql.query(`with claim as(select j.id from document_jobs j join opportunity_documents d on d.id=j.document_id join opportunities o on o.id=d.opportunity_id where j.stage='analyze' and j.state='pending' and j.run_after<=now() and d.extraction_status in('text_extracted','analyzed') and lower(coalesce(o.status,'')) in ('open','active','posted') and (o.due_at is null or o.due_at>=now()) order by j.priority,j.run_after,j.id limit 24 for update skip locked),leased as(update document_jobs j set state='leased',leased_until=now()+interval '10 minutes',lease_owner=$1,attempts=attempts+1,updated_at=now() from claim where j.id=claim.id returning j.id job_id,j.document_id) select leased.job_id::text,d.id,d.opportunity_id,d.filename,ef.normalized_value->>'text_storage_key' text_storage_key from leased join opportunity_documents d on d.id=leased.document_id join extracted_facts ef on ef.document_id=d.id and ef.fact_type='document_text_extract' where ef.normalized_value->>'text_storage_key' is not null`,[owner]) as Row[];if(!rows.length)return NextResponse.json({ok:true,processed:0,message:"No analysis jobs are waiting",analyzerVersion:ANALYZER_VERSION});const results=[] as Array<Record<string,unknown>>;const concurrency=6;for(let i=0;i<rows.length;i+=concurrency)results.push(...await Promise.all(rows.slice(i,i+concurrency).map(analyzeOne)));const analyzed=results.filter(r=>r.ok).length;return NextResponse.json({ok:true,processed:results.length,analyzed,failed:results.length-analyzed,requirementsFound:results.reduce((sum,r)=>sum+Number(r.requirementsFound||0),0),analyzerVersion:ANALYZER_VERSION})}
