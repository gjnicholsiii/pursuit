import { NextRequest, NextResponse } from "next/server";
import { getSql } from "@/lib/db";
import { requireInternalAuth } from "@/lib/internal-auth";
export const dynamic="force-dynamic"; export const maxDuration=300;
const CAMPAIGN="campus-security-advisory-v1", SIZE=500;
const first=(s:string)=>(s||"").trim().split(/\s+/)[0]||"there";
function eligible(t:string,f:string){t=(t||"").toLowerCase();if(/principal|teacher|board|finance|financial|procurement|purchasing|facilit|maintenance|vendor/.test(t))return false;if(f==="Executive")return /\b(superintendent|assistant superintendent|deputy superintendent)\b/.test(t);if(f==="Security")return /(chief|director|executive director|manager).*(security|safety|school safety|public safety|emergency management)|(security|safety|school safety|public safety|emergency management).*(chief|director|executive director|manager)/.test(t);if(f==="Technology")return /\b(cio|cto)\b|director.*(it|information technology|technology|information systems|infrastructure|network services)|(it|technology) manager|chief.*(information|technology)/.test(t);return false;}
function copy(n:string,i:string){return `Hi ${n},\n\nI spent years on the bidder side of school security projects, which taught me exactly where security programs become expensive, difficult to maintain, or obsolete long before anyone intended. Blackvane now works from the owner side, helping districts make those decisions before an RFP locks them in.\n\nWe provide independent security assessment, system design, procurement support, and project oversight. A major part of that work is a ten-year security technology roadmap for ${i}, built around viability, survivability, lifecycle planning, and preventing overspending as systems age and requirements change.\n\nWe also offer a pre-RFP Bidder's Review: an independent review from the perspective of the companies that will eventually price and respond to the package, before it reaches the street. That catches ambiguity, unnecessary cost, scope conflicts, and procurement problems while they are still inexpensive to correct.\n\nIf a conversation would be useful, I'd be glad to compare notes.\n\nJoe Nichols\nBlackvane 13\nblackvane13.com`;}
export async function GET(req:NextRequest){const auth=requireInternalAuth(req);if(auth)return auth;const sql=getSql();const c=await sql.query(`select count(*)::int n from raven_outreach_sends s join raven_outreach_batches b on b.id=s.batch_id where b.campaign=$1 and s.status='sent'`,[CAMPAIGN]) as any[];const total=Number(c[0]?.n||0);if(total>=1000)return NextResponse.json({ok:true,complete:true,sent:total});const rows=await sql.query(`select distinct on(lower(rp.email)) rp.id::text person_id,rp.full_name,rp.title,rp.role_family,rp.email,a.canonical_name institution from raven_people rp join agencies a on a.id=rp.agency_id where a.agency_type='k12' and rp.email is not null and btrim(rp.email)<>'' and rp.role_family in('Executive','Security','Technology') and not exists(select 1 from raven_outreach_sends s where lower(s.email)=lower(rp.email) and s.status='sent') order by lower(rp.email),rp.confidence desc nulls last,rp.last_verified_at desc nulls last`) as any[];const q=rows.filter(r=>eligible(r.title,r.role_family));if(q.length<SIZE)return NextResponse.json({ok:true,skipped:true,qualified:q.length,needed:SIZE});const bn=Math.floor(total/500)+1;const br=await sql.query(`insert into raven_outreach_batches(campaign,batch_number,status,target_count) values($1,$2,'sending',500) on conflict(campaign,batch_number) do update set status='sending' returning id::text`,[CAMPAIGN,bn]) as any[];const bid=br[0].id,batch=q.slice(0,SIZE);for(const r of batch)await sql.query(`insert into raven_outreach_sends(batch_id,person_id,email,status,first_name,full_name,institution,title) values($1,$2,$3,'pending',$4,$5,$6,$7) on conflict(batch_id,person_id) do nothing`,[bid,r.person_id,r.email,first(r.full_name),r.full_name,r.institution,r.title]);const key=process.env.RESEND_API_KEY,from=process.env.BLACKVANE_OUTBOUND_FROM||process.env.OUTREACH_FROM_EMAIL;if(!key||!from)return NextResponse.json({ok:false,error:'Blackvane outbound sender is not configured for Raven',batch:bn},{status:500});let sent=0,failed=0;for(const r of batch){try{const x=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json'},body:JSON.stringify({from,to:[r.email],subject:'A bidder-side review before your next security RFP',text:copy(first(r.full_name),r.institution)})});const b=await x.json().catch(()=>({}));if(!x.ok)throw new Error(b?.message||`Resend ${x.status}`);await sql.query(`update raven_outreach_sends set status='sent',provider_message_id=$3,sent_at=now(),error=null where batch_id=$1 and person_id=$2`,[bid,r.person_id,b.id||null]);sent++;}catch(e){failed++;await sql.query(`update raven_outreach_sends set status='failed',error=$3 where batch_id=$1 and person_id=$2`,[bid,r.person_id,e instanceof Error?e.message:String(e)]);}}await sql.query(`update raven_outreach_batches set status=$2,sent_count=$3,failed_count=$4,completed_at=now() where id=$1`,[bid,failed?'partial':'sent',sent,failed]);return NextResponse.json({ok:true,batch:bn,sent,failed,totalSuccessful:total+sent});}
